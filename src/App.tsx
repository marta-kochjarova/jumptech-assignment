import React from 'react';
import RoutesConfig from './routes';
import Layout from './components/Layout';

const App = () => {
  return (
     <Layout>
      <RoutesConfig />
    </Layout>
  );
};

export default App;
